import React from "react";

function Navbar() {
    return (
        <nav className="navbar sticky">
            <div>
                <ul className="navbar-nav">
                    <h2>New Portfolio</h2>
                    {/* add in Name on the left side, links for about, portfolio and contact on the right
                    need to add link for resume too */}
                </ul>
            </div>
        </nav>
    )
}

export default Navbar;